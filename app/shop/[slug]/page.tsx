import Link from 'next/link';
import {notFound} from 'next/navigation';
import {products,money} from '../../../lib/products';
import {PageHero,Enquire} from '../../components/Page';
import AddToBag from '../../components/AddToBag';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(p=>p.slug===slug);return {title:p?.name||'Product not found'}}
export default async function Product({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(p=>p.slug===slug);if(!p)notFound();return <main><PageHero eyebrow={p.category.toUpperCase()} title={p.name}>{p.description}</PageHero><section className="container section split"><div><Link className="backLink" href="/shop">← Back to shop</Link><h2>{money(p.price)}</h2><p>Illustrative catalogue price</p><div className="notice">Confirm the exact model, approved specifications, current price and availability before purchase.</div><div className="actions"><AddToBag slug={p.slug}/><Link className="textLink" href="/bag">View bag →</Link></div></div><div className="card"><h2>Choose with the right information.</h2><p>Ask about what is included, device compatibility, warranty and support for your intended use.</p><Enquire topic={p.name} label="Ask about this product"/></div></section><section className="careBand"><div className="container split"><h2>Supported beyond<br/>the purchase.</h2><div><p>Explore servicing, calibration and technical support for your equipment.</p><Link className="button light" href="/services">Explore biomedical services →</Link></div></div></section></main>}
